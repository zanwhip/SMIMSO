# ========================================
# ✅ BẮT BUỘC - REQUIRED
# ========================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Supabase Configuration (Lấy từ: https://app.supabase.com → Settings → API)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# JWT Configuration (Tạo random string dài, ví dụ: my-super-secret-key-123456)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Frontend URL (Để CORS hoạt động)
FRONTEND_URL=http://localhost:3000

# ========================================
# ❌ KHÔNG BẮT BUỘC - OPTIONAL
# ========================================

# Google OAuth (Chỉ cần nếu dùng Google login - có thể để trống)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# AI Service (Chỉ cần nếu dùng AI features - có thể để trống)
# Nếu chạy AI Service local: http://localhost:8000
# Nếu không dùng AI, để trống - backend vẫn chạy bình thường
AI_SERVICE_URL=http://localhost:8000

# Upload Configuration (Đã có default trong code - có thể bỏ)
# MAX_FILE_SIZE=10485760
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg,image/webp

